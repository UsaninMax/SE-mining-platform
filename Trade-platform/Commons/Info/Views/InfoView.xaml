<Window x:Class="TradePlatform.Commons.Info.Views.InfoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TradePlatform.Commons.Info.Views"
      mc:Ignorable="d" 
      Title="Info:" Height="300" Width="1000">

    <TabControl SelectedIndex="0"
        ItemsSource="{Binding Tabs}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock
                    Text="{Binding Id}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate>
                <DataGrid IsReadOnly="True" AutoGenerateColumns="False" ItemsSource="{Binding Messages}">
                    <DataGrid.Columns>
                        <DataGridTextColumn  Header="Date" Binding="{Binding Date}" />
                        <DataGridTextColumn  Width="*" Header="Message" Binding="{Binding Message}" />
                    </DataGrid.Columns>
                </DataGrid>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</Window>
