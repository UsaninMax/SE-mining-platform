<UserControl x:Class="TradePlatform.StockDataUploud.view.DownloadedDataView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         mc:Ignorable="d" 
         d:DesignHeight="100">
    <Grid>
        <ListView ItemsSource="{Binding DounloadedInstruments}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Instrument" DisplayMemberBinding="{Binding Instrument}" />
                    <GridViewColumn Header="From" DisplayMemberBinding="{Binding From}" />
                    <GridViewColumn Header="To" DisplayMemberBinding="{Binding To}" />
                    <GridViewColumn Header="Step" DisplayMemberBinding="{Binding Step}" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
    
</UserControl>
